---
id: "239"
title: 滑动窗口最大值
englishTitle: Sliding Window Maximum
slug: sliding-window-maximum
url: https://leetcode-cn.com/problems/sliding-window-maximum/
difficulty: Hard
tags:
  - 子串
  - 队列
  - 数组
  - 滑动窗口
  - 单调队列
  - 堆（优先队列）
---

## 题面

给你一个整数数组 `nums`，有一个大小为 `k`   的滑动窗口从数组的最左侧移动到数组的最右侧。你只可以看到在滑动窗口内的 `k` 个数字。滑动窗口每次只向右移动一位。

返回 滑动窗口中的最大值 。

示例 1：

    输入： nums = [1,3,-1,-3,5,3,6,7], k = 3
    输出：[3,3,5,5,6,7]
    解释：
    滑动窗口的位置                最大值
    ---------------               -----
    [1  3  -1] -3  5  3  6  7       3
     1 [3  -1  -3] 5  3  6  7       3
     1  3 [-1  -3  5] 3  6  7      5
     1  3  -1 [-3  5  3] 6  7       5
     1  3  -1  -3 [5  3  6] 7       6
     1  3  -1  -3  5 [3  6  7]      7

示例 2：

    输入： nums = [1], k = 1
    输出：[1]

提示：

  * `1 <= nums.length <= 105`
  * `-104 <= nums[i] <= 104`
  * `1 <= k <= nums.length`

## 分析

- 在滑动窗口时，只需要维护一个单调队列，队首大于队尾元素

- 当遇到新元素时，弹出队尾所有大于新元素的值

- 当滑动窗口时，如果队首元素就是即将要被移出窗口的元素，那么就弹出队首元素

	- 如果被移除的是当前窗口的最大值，那么一定在队首
	- 如果被移除的不是当前窗口的最大值，那么一定有 m>i，且 nums[m]>nums[i]，当 m 入队时，那么一定会将 i 被弹出

- 代码实现

	```cpp
	class Solution {
	public:
	    vector<int> maxSlidingWindow(vector<int>& nums, int k) {
	        std::deque<int> window;
	        vector<int> windowMax = vector(nums.size() - k + 1, 0);
	        for (int i = 0, j = 0; j < nums.size(); j++) {
	            int window_size = j - i + 1;
	            while (!window.empty() && nums[window.back()] < nums[j]) {
	                window.pop_back();
	            }
	            window.push_back(j);
	            if (window_size == k) {
	                windowMax[i] = nums[window.front()];
	                if (window.front() == i) {
	                    window.pop_front();
	                }
	                i++;
	            }
	        }
	        return windowMax;
	    }
	};
	```

	
